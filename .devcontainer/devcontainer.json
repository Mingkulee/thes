{
  "name": "thes — Telegram Multi-Agent Bot",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bookworm",
  "postCreateCommand": "pip install --user -r requirements.txt && sudo apt-get update -qq && sudo apt-get install -y -qq libreoffice-writer --no-install-recommends || true",
  "postAttachCommand": "bash .devcontainer/welcome.sh",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  },
  "remoteEnv": {
    "PYTHONUNBUFFERED": "1"
  },
  "containerEnv": {
    "TELEGRAM_BOT_TOKEN": "${localEnv:TELEGRAM_BOT_TOKEN}",
    "TELEGRAM_BOT_TOKEN_TRANSLATOR": "${localEnv:TELEGRAM_BOT_TOKEN_TRANSLATOR}",
    "TELEGRAM_BOT_TOKEN_CODER": "${localEnv:TELEGRAM_BOT_TOKEN_CODER}",
    "TELEGRAM_BOT_TOKEN_RESEARCHER": "${localEnv:TELEGRAM_BOT_TOKEN_RESEARCHER}",
    "TELEGRAM_BOT_TOKEN_WEATHER": "${localEnv:TELEGRAM_BOT_TOKEN_WEATHER}",
    "TELEGRAM_BOT_TOKEN_DOCANALYST": "${localEnv:TELEGRAM_BOT_TOKEN_DOCANALYST}",
    "TELEGRAM_BOT_TOKEN_GMAIL": "${localEnv:TELEGRAM_BOT_TOKEN_GMAIL}",
    "GOOGLE_API_KEY": "${localEnv:GOOGLE_API_KEY}",
    "GMAIL_CLIENT_ID": "${localEnv:GMAIL_CLIENT_ID}",
    "GMAIL_CLIENT_SECRET": "${localEnv:GMAIL_CLIENT_SECRET}",
    "GMAIL_TOKEN_JSON": "${localEnv:GMAIL_TOKEN_JSON}"
  }
}
